import 'package:flutter/material.dart';

class PrimaryFilePicker extends StatelessWidget {
  const PrimaryFilePicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}